import RegisterContainer from '@/src/screens/auth/register/register.container'
import React from 'react'
import { View } from 'react-native'

function RegisterScreen() {
  return (
    <RegisterContainer/>
  )
}

export default RegisterScreen
